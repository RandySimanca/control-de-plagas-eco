server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        
        # Redirigir el enrutamiento a index.html para que React Router funcione (SPA)
        try_files $uri $uri/ /index.html;
    }
}
