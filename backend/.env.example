PORT=3001
NODE_ENV=development

# FRONTEND_URL es crítico para CORS.
# En desarrollo: http://localhost:5173
# En producción: https://tu-dominio-real.com
FRONTEND_URL=http://localhost:5173

# PostgreSQL
DATABASE_URL=postgres://postgres:postgres@localhost:5432/plagcontrol

# JWT
JWT_SECRET=cambia_esto_por_un_secreto_largo
# TODO (v2.0 - Deuda Técnica): Implementar Refresh Tokens.
JWT_EXPIRES_IN=1d
PORT=3001
NODE_ENV=development

# PostgreSQL connection string
DATABASE_URL=postgresql://usuario:password@localhost:5432/plagcontrol

# Required for JWT signing (use a long random string in production)
JWT_SECRET=cambia_esto_por_un_secreto_largo_y_aleatorio
JWT_EXPIRES_IN=7d
